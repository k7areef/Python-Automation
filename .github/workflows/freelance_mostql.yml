name: Run Freelance Mostql Script

on:
  workflow_dispatch:
  repository_dispatch:
    types: [Run Freelance Mostql Script]

jobs:
  run-all:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - run: pip install -r requirements.txt

      - name: Run Freelance Mostql Script
        env:
          TELEGRAM_TOKEN_CVE: ${{ secrets.TELEGRAM_TOKEN_FREELANCE }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          MONGO_URI: ${{ secrets.MONGO_URI }}
        run: |
          python -m scripts.cve.nvd_api
